# Windows Release Files

- `JoyCaptionGUI-Setup.exe`: Windows bootstrap installer.
- `JoyCaptionGUI-Payload.zip`: Local source payload used by the installer when placed beside the setup executable.

Keep both files in the same folder for the most reliable install path. If the payload zip is absent, the installer attempts to download the source from GitHub.
